**Placeholders:**
- Keep placeholders like `{0}`, `{1}`, `{2}`, `{3}`, `{target}`, `{location}`, '{inventory}`, `{name_1}`, and `{name_2}` unchanged.
- Treat `{0}`, `{1}`, `{2}`, `{3}` as objects based on context.
- Treat `{name_1}` as a last name placeholder based on context.
- Treat `{name_2}` as a first name placeholder based on context.