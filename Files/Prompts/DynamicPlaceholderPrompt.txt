**Placeholders:**
- Keep all placeholders like `{0}`, `{1}`, `{2}`, `{3}` unchanged.
- Keep all text that starts with a '{' and ends with a '}' completely unchanged. 
- Treat placeholders as objects based on context.